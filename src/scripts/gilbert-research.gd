extends Node2D

var bookNode = preload("res://src/scenes/book.tscn")
# Called when the node enters the scene tree for the first time.
func _ready():
	
	var book_instance = bookNode.instantiate()
	book_instance.set_meta("Page1", "Historical Significance, Blurb Drainage systems date back to 4,000 BCE in Mesopotamia and Iran.")
	book_instance.set_meta("Page2", "This is the practice of adding dedicated ditches and areas for water to pool to reduce risks of floods and storms ruining crops.
")
	self.add_child(book_instance)

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
	
func _unhandled_input(event):
	if event is InputEventMouseButton:
		if event.is_pressed():
			get_tree().change_scene_to_file("res://src/scenes/main.tscn")
